import 'package:flutter/cupertino.dart';

class SpecialArea extends StatelessWidget {
  const SpecialArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("special area");
  }
}