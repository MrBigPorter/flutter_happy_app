import 'package:flutter/cupertino.dart';

class Page403 extends StatelessWidget {
  const Page403({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('403 - Forbidden'),
    );
  }
}